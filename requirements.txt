# Data preprocessing
datasets>=2.19.0
pandas>=2.0.0
pyarrow>=14.0.0

# Training
# Install PyTorch first from https://pytorch.org/get-started/locally/ (choose CUDA version)
# e.g. pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
transformers>=4.40.0
trl>=0.9.0
accelerate>=0.27.0

# Evaluation
# unitary/toxic-bert is loaded via the transformers pipeline; no extra package needed
